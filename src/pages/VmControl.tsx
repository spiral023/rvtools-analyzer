import { PageHeader } from "@/components/layout/PageHeader";

/** Platzhalter für künftige VM-Kontrollen. */
export default function VmControl() {
  return (
    <div className="animate-fade-in">
      <PageHeader title="VM-Kontrolle" />
    </div>
  );
}
